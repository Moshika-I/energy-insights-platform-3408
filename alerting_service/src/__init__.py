"""Root package for alerting_service.

This file ensures the `src` directory is treated as a Python package so imports like
`from src.api.main import app` work in all execution contexts (uvicorn, scripts, tests).
"""
